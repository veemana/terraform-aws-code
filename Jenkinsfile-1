pipeline {
    agent {
        docker {
            image 'hashicorp/terraform:1.0.3'
        }
    }

    stages {
      stage('Checkout') {
        steps {
          checkout scm
          sh 'terraform init'
          }
        }
      }
    }
